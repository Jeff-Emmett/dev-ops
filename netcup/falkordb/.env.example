# Copy to .env on Netcup at /opt/apps/falkordb/.env (chmod 600)
# Real password lives in Infisical; this file is just the deployment shim.
# Migrate to Infisical entrypoint-wrapper pattern when convenient — same
# approach as vaultwarden / other third-party services in this dev-ops repo.

FALKORDB_PASSWORD=replace_me_with_a_long_random_string
