services:
  postiz:
    image: ghcr.io/gitroomhq/postiz-app:latest
    container_name: postiz
    restart: unless-stopped
    entrypoint: ["/infisical-entrypoint.sh"]
    command: ["docker-entrypoint.sh", "sh", "-c", "nginx && pnpm run pm2"]
    environment:
      # === Infisical secret injection ===
      INFISICAL_CLIENT_ID: "${INFISICAL_CLIENT_ID}"
      INFISICAL_CLIENT_SECRET: "${INFISICAL_CLIENT_SECRET}"
      INFISICAL_PROJECT_SLUG: "postiz-main"
      INFISICAL_ENV: "prod"
      INFISICAL_URL: "http://infisical:8080"
      # === Config (not secrets) ===
      MAIN_URL: "https://demo.rsocials.online"
      FRONTEND_URL: "https://demo.rsocials.online"
      NEXT_PUBLIC_BACKEND_URL: "https://demo.rsocials.online/api"
      DATABASE_URL: "postgresql://postiz:${POSTGRES_PASSWORD}@postiz-postgres:5432/postiz"
      REDIS_URL: "redis://postiz-redis:6379"
      BACKEND_INTERNAL_URL: "http://localhost:3000"
      TEMPORAL_ADDRESS: "postiz-temporal:7233"
      IS_GENERAL: "true"
      DISABLE_REGISTRATION: "false"
      STORAGE_PROVIDER: "local"
      UPLOAD_DIRECTORY: "/uploads"
      NEXT_PUBLIC_UPLOAD_DIRECTORY: "/uploads"
      # === OAuth config (client_id/secret come from Infisical) ===
      POSTIZ_GENERIC_OAUTH: "true"
      NEXT_PUBLIC_POSTIZ_OAUTH_DISPLAY_NAME: "EncryptID"
      NEXT_PUBLIC_POSTIZ_OAUTH_LOGO_URL: ""
      POSTIZ_OAUTH_URL: "https://auth.ridentity.online"
      POSTIZ_OAUTH_AUTH_URL: "https://auth.ridentity.online/oidc/authorize"
      POSTIZ_OAUTH_TOKEN_URL: "https://auth.ridentity.online/oidc/token"
      POSTIZ_OAUTH_USERINFO_URL: "https://auth.ridentity.online/oidc/userinfo"
      # === Email config (EMAIL_PASS comes from Infisical) ===
      EMAIL_PROVIDER: "nodemailer"
      EMAIL_FROM_NAME: "rSocials"
      EMAIL_FROM_ADDRESS: "noreply@rmail.online"
      EMAIL_HOST: "mailcowdockerized-postfix-mailcow-1"
      EMAIL_PORT: "587"
      EMAIL_SECURE: "false"
      EMAIL_USER: "noreply@rmail.online"
      NODE_TLS_REJECT_UNAUTHORIZED: "0"
      API_LIMIT: 30
      NX_ADD_PLUGINS: false
    volumes:
      - postiz-config:/config/
      - postiz-uploads:/uploads/
      - /opt/infisical/entrypoint-wrapper.sh:/infisical-entrypoint.sh:ro
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.postiz.rule=Host(`demo.rsocials.online`)"
      - "traefik.http.routers.postiz.entrypoints=web,websecure"
      - "traefik.http.services.postiz.loadbalancer.server.port=5000"
      - "traefik.docker.network=traefik-public"
    networks:
      - traefik-public
      - postiz-internal
      - mailcow-network
    depends_on:
      postiz-postgres:
        condition: service_healthy
      postiz-redis:
        condition: service_healthy
      postiz-temporal:
        condition: service_started

  postiz-postgres:
    image: postgres:17-alpine
    container_name: postiz-postgres
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD}"
      POSTGRES_USER: postiz
      POSTGRES_DB: postiz
    volumes:
      - postiz-postgres-data:/var/lib/postgresql/data
    networks:
      - postiz-internal
    healthcheck:
      test: pg_isready -U postiz -d postiz
      interval: 10s
      timeout: 3s
      retries: 3

  postiz-redis:
    image: redis:7.2
    container_name: postiz-redis
    restart: unless-stopped
    healthcheck:
      test: redis-cli ping
      interval: 10s
      timeout: 3s
      retries: 3
    volumes:
      - postiz-redis-data:/data
    networks:
      - postiz-internal

  postiz-temporal:
    image: temporalio/auto-setup:1.28.1
    container_name: postiz-temporal
    restart: unless-stopped
    depends_on:
      - postiz-temporal-wait
    environment:
      - DB=postgres12
      - DB_PORT=5432
      - POSTGRES_USER=temporal
      - POSTGRES_PWD=temporal
      - POSTGRES_SEEDS=temporal-shared-postgres
      - DBNAME=temporal_main
      - VISIBILITY_DBNAME=temporal_visibility_main
      - ENABLE_ES=true
      - ES_SEEDS=temporal-shared-elasticsearch
      - ES_VERSION=v7
      - TEMPORAL_NAMESPACE=default
    networks:
      - postiz-internal
      - postiz-temporal-shared

  # Tiny wait container to ensure shared infra is reachable before Temporal starts
  postiz-temporal-wait:
    image: postgres:16
    container_name: postiz-temporal-wait
    restart: "no"
    entrypoint: ["sh", "-c", "until pg_isready -h temporal-shared-postgres -U temporal; do sleep 2; done"]
    networks:
      - postiz-temporal-shared

volumes:
  postiz-postgres-data:
  postiz-redis-data:
  postiz-config:
  postiz-uploads:

networks:
  traefik-public:
    external: true
  postiz-internal:
    driver: bridge
  mailcow-network:
    external: true
    name: mailcowdockerized_mailcow-network
  postiz-temporal-shared:
    external: true
    name: shared-temporal_postiz-temporal-shared
