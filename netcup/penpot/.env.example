# Copy to .env on Netcup and fill with generated secrets. NEVER commit .env.
#   PENPOT_SECRET_KEY=$(openssl rand -hex 32)
#   POSTGRES_PASSWORD=$(openssl rand -hex 24)
PENPOT_SECRET_KEY=
POSTGRES_PASSWORD=
