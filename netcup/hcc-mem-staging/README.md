# HCC Member Staging

**Canonical repo:** `/home/jeffe/Github/hcc-mem-staging/`

This copy in `dev-ops/netcup/` is the server-side deployment reference.
The standalone repo is the primary development location.

## Setup

Once the Gitea repo is created:

```bash
cd ~/Github/hcc-mem-staging
git remote add origin git@gitea.jeffemmett.com:jeff/hcc-mem-staging.git
git push -u origin main
```
