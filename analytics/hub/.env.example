# Read-only Umami DB connection for the hub. Real .env lives on Netcup, gitignored.
# Role: umami_ro (SELECT-only). Create/rotate via analytics/README.md.
DATABASE_URL=postgresql://umami_ro:CHANGEME@umami-db:5432/umami
